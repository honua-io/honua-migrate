# Run-004: dimensional/date imports and the Editor backend

This exploratory run fixes source-ordinate capture, feature-type publication and
dimensional metadata, then repairs date-only storage and query behavior. It does
not complete or qualify the four-app conversion. [Machine-readable observations](run-004.json)
retain every recorded job and the available command timings, including failures.

## Environment and method

A fresh local database and separate Redis instance isolate this run from the
user's Powerplants demo. Service movement uses the Honua server importer through
`honua-migrate services arcgis plan/apply/resume`; Python only orchestrates and
independently compares source/target responses. Source Esri services remain read-only.
The CLI terminal-status fix is commit f822262 (PR #157).

The managed host comes from server 93d75f2eb, with four explicitly rebuilt
assemblies from 04f7e4c78; the dimensional flight retry updates Core/Postgres with the
dimensional-publication changes committed as eb754214b. Private manifests retain
DLL hashes, the source patch and exact runtime provenance. This is component-overlay
evidence, not a fresh full-host build. The initial host start failed because the
new database lacked PostGIS; provisioning the extension fixed that preflight.

## Service results

| Resource / attempt | Rows committed | Job verdict | Server duration | Target layer |
| --- | ---: | --- | ---: | ---: |
| Hazards | 4,246 | NeedsReview | 26.858 s | 2 |
| Roads | 35 | NeedsReview | 1.651 s | 3 |
| Areas | 49 | NeedsReview | 3.644 s | 4 |
| Flights, source-ordinate fix | 1,963 | NeedsReview | 10.476 s | 1 |
| Flights, publication fix | 1,963 | NeedsReview | 9.364 s | 8 |
| Census polygons | 579 | Completed | 4.553 s | 5 |
| Complaint points | 22,587 | NeedsReview | 100.343 s | 6 |
| Related summary table | 0 (rolled back) | Failed | 3.147 s | none |
| Summary's parent polygons | 579 | Completed | 4.726 s | 7 |

These are run observations, not controlled performance comparisons. The flight
profile explicitly selects Jan 11, 2026; the full 698,843-row flight source remains
unqualified. Other dependencies are imported without that flight-date restriction.
The parent's Completed verdict does not establish relationship fidelity while
its related table is missing.

## Reusable improvements and evidence

- Server [PR #4830](https://github.com/honua-io/honua-server/pull/4830) preserves
  all 8/2/2 Editor feature types, their normalized type fields and valid prototype
  field bindings. Full template presentation and unsupported diagnostics remain open.
- Final flight readback advertises `hasZ=true`. A fresh independent comparison
  matches all 1,963 natural keys, every non-OID attribute and path/vertex topology.
  Across 3,926 vertices, maximum XY delta is below 4.50e-10 degrees and Z delta
  below 7.30e-12. Source captures are mutable, not a frozen benchmark dataset.
- Six real PostGIS tests pass for both subtype encodings and 2D/Z/M/ZM points.
  The first run caught a POINTM publishing failure; topology normalization fixes it.
  Changed-project builds and scoped formatting pass. Broad pre-pr/architecture
  gates were not run locally.
- Editor's first browser start failed by attaching Terra Draw to a read-only source.
  The app now checks `applyEdits` before binding drawing. Installed-SDK browser
  checks cover all three row counts, layer switching, selection without geometry,
  area reload, a 20-of-49 Open search, and rendered OpenFreeMap attribution.
  The subsequent production build passes in 5.376 seconds; these browser checks
  used the development server. CRUD and attachment writes remain unverified.

## Next onboarding fixes

1. [Date-only fidelity (#4833)](https://github.com/honua-io/honua-server/issues/4833):
   Server PR #4839 repairs the public date path; the fresh retry below records parity.
   SDK [#1718](https://github.com/honua-io/honua-sdk-js/issues/1718) still needs date-only
   discovery and PBF type preservation before typed calendar queries are qualified.
2. [Table and relationship import (#4834)](https://github.com/honua-io/honua-server/issues/4834):
   unconditional spatial indexing references a nonexistent geom column. Publication
   also requires geometry. Preserve tables and target relationship IDs without dummy shapes.
3. [Editing publication (#4825)](https://github.com/honua-io/honua-server/issues/4825):
   Editor imports remain Query-only and omit attachment/GlobalID capability bindings.
4. [Source-aware reconciliation (#4826)](https://github.com/honua-io/honua-server/issues/4826):
   compare actual source geometry/nulls/extents. Complaint extent differs from source
   metadata by 8.5%; target Editor null counts are 15/7/2 and need independent source comparison.

The development service URL contains spaces; the first plan failed validation,
then percent-encoded paths planned successfully. Keep both receipts. Normalization
guidance can eliminate this manual intervention without weakening URL validation.

## Reuse and metrics limits

### Fresh date-only retry

Server [PR #4839](https://github.com/honua-io/honua-server/pull/4839), commit
b39148d9d, stores Esri DateOnly as PostgreSQL DATE, rejects invalid source dates,
and preserves calendar semantics in GeoServices JSON/PBF and Arrow Date32. Timestamp
epochs remain unchanged. Seven PostGIS tests and 97 protocol tests pass, plus
changed-component builds and scoped formatting. The provider test first failed
because the invalid date was accepted as text; that receipt is retained.

Fresh job abccf487f1c5 imports 1,963 rows to layer 9 in 5.587 seconds with zero
row failures. Both original-case and normalized DATE filters return 1,963; metadata
advertises DateOnly and hasZ=true. Every natural key and non-OID attribute matches
a fresh Esri readback. All 3,926 vertices retain topology and dimensionality,
with maximum XY delta 4.49e-10 degrees and Z delta 7.30e-12.

The job remains NeedsReview: internal count/extent/sample reconciliation still
uses a raw-WHERE parser that rejects DATE literals, and its source count is the
unfiltered 698,843 rows. These findings refine #4826; the public date query succeeds.
SDK 0.1.9-beta.0 separately discovers Esri DateOnly as unknown, and its PBF decoder
falls back to String for enum 14 (#1718). A live source PBF capture confirms enum
14 with ISO date strings. Installed SDK 0.1.9-beta.0 readback passes all 1,963 imported rows using the original
native DATE filter, with zero date/timestamp errors. Typed discovery still returns
unknown for both dates, so typed semantic queries remain unqualified (#1718).
A dependency restore was required after local cleanup removed the app's installed
SDK files; the restored lockfile now lives in an external local cache.

The current runtime keeps the recorded 93d75f2eb host with five rebuilt components
(Core.Abstractions, Core, Postgres, Hosting, GeoServices), whose source patch and
hashes are private. This is not a full fresh-host build. All ten job receipts and
the available 33 recorded command receipts retain their failures. Build/test/format
timings are recorded separately and do not include all agent work or queue waits.

### Cached state

A private PostgreSQL custom dump, runtime/configuration manifests and raw captures
are cached outside source control. The retained Redis AOF volume is separate.
Restore, attachment-asset completeness and immutable-source qualification have
not run; this is an exploratory checkpoint, not an admitted snapshot baseline.
The latest dump includes the date-only retry (10,895,321 bytes, SHA-256
`39b70bd564b455e751c51c73a9c54cc044e3f238f82328a390ac22435d8b79d3`).

The metrics distinguish command exit status from import fidelity. Nonzero resume
exits for NeedsReview/Failed are correct CLI behavior. Available stage timings
exclude unmetered agent work and build-queue wait. Model cost/tokens are unavailable,
not zero. Fresh agent conversion repeats, complete browser behavior and full
backend fidelity still need their own evidence.


### SDK calendar-date regression baseline

[SDK PR #1720](https://github.com/honua-io/honua-sdk-js/pull/1720), candidate
6832c8c21, maps DateOnly during schema discovery and PBF decoding. Before the
production change, the required build passed and the three targeted suites
reported 180 passing tests, four product regression failures and one cache-fixture
setup failure. The fixture was corrected before the production fix. The command
took 279.608 seconds excluding its queue wait. Its receipt and log survived a
subsequent Python console-encoding error in the private reporting wrapper.
The root dependency install took 36.894 seconds; queue time is not measured.

A fresh installed published-package check against original Esri metadata confirms
the unknown logical date type. Post-fix gates and separate installed candidate
source/target JSON/PBF readback are pending. No candidate qualification is claimed.


### Installed SDK candidate exposed additional PBF loss

Candidate 6832c8c21 passed all 185 targeted tests, typecheck (142.730 seconds)
and Biome (5.664 seconds). The test/build command took 271.346 seconds. Packing
took 12.105 seconds and a separate consumer install took 40.959 seconds.
The candidate tarball SHA-256 is
`6e72e681d2b13ebe7b4ad29dad3662e3aabc818c530d7971d674fea20d7de508`;
it retains package version 0.1.9-beta.0 but is not the registry artifact.

Live original-source JSON readback passes the discovered-schema DATE query for
1,963 records. PBF returns the same row count and DateOnly metadata but empty
attribute objects. Actual ArcGIS values omit optional field indexes and encode
start_t with signed zigzag tag 8; earlier synthetic fixtures always supplied
indexes and used tag 6. Both first-candidate failures and diagnostic receipts
are retained privately. No source/target PBF parity is claimed.

Follow-up e77171323 in SDK PR #1720 handles these encodings and adds regressions
for null positions, explicit sparse indexes, negative timestamps and signed-64
boundaries. Its gates and second installed-candidate readback are pending. This
is a concrete example of why installed live onboarding tests complement local
fixtures. All timings exclude queue waits and unmetered agent work.


### SDK candidate verified against both backends

Follow-up e77171323 passes Biome (3.756 seconds), typecheck (44.459 seconds) and
all 195 targeted tests; the prepared-build/test command takes 111.150 seconds.
Its separate tarball takes 5.907 seconds to pack and 28.945 seconds to install.
Artifact SHA-256:
`7d98b2b02d3ecd31dcde6a6af44e23b15ce59fee6f19124e15ee266199bc7686`.

Installed discovered-schema day queries now pass against original Esri and fresh
Honua layer 9 in JSON and PBF. Each returns 1,963 unique natural keys with matching
DateOfFlight/dateonlyutc strings and start_t/end_t epochs. All selected-row hashes
are `5462ad03a1bd175ad84cd881b67e72c6abd0dda810dd839010db7b087c9be8ef`.
Twelve comparisons pass across Node processes in Honolulu, UTC and Kiritimati
(UTC+14), with no PBF-to-JSON fallback. The three readback runs take 2.843, 2.813
and 2.834 seconds. Geometry is omitted from this specific format check; the
previous independent geometry evidence remains separate.

SDK PR #1720 and coordinated server PR #4839 are ready for review. Candidate
qualification does not imply a registry release, full-volume import or completed
app/browser conversion. Source-aware reconciliation #4826, nonspatial table
publication #4834, Editor CRUD/attachments and native helicopter UI still remain.


### Helicopter UI source checkpoint

SDK commit `5551fa415` adds the fourth application's native UI source. The
available-day calendar, grouped aircraft list, selected tracks, complaints
list/extent filter, layer toggles and click-based neighborhood statistics use
Honua source queries and geometry operations. This is **not yet a runnable or
qualified conversion**: dependency installation, lockfile, typecheck, production
build and browser evidence remain pending behind the shared server build.

Root Biome passes 1,450 files and the app-specific check passes six files using
the previously installed pinned Biome executable. Live metadata readback confirms
four spatial bindings: flights 9, complaints 6, census 5 and summary 7. It also
exposes a concrete conversion correction: the complaint-description column is
`problem_detail__formerly_descriptor_`, rather than `Descriptor`. The UI now
handles that reviewed field name. The SDK geometry intersection combines the
viewport and corridor polygons for complaint points, preserving both spatial
constraints within GeoServices' single geometry operand.

The current flight import covers January 11, 2026 only (year corrected by the source and imported readbacks below). Full-range data, the
related nonspatial table, pointer-following lens, proportional timeline and
selection-time effects, thematic summary rendering and chart metadata adoption
remain explicit parity gaps in the app README. Browser performance measures for
flight and complaint query runs are instrumented, but no runtime timings are
claimed until those workflows execute. See SDK draft PR #1714.


### Helicopter chart and first browser run (6ce0bf3f2)

The fourth app now has a committed lockfile and builds from installed packages.
Its revised Biome check passes 10 files, all four chart tests pass, and the
TypeScript/Vite production build passes in 12.029 seconds (632.60 kB gzip main
chunk). Initial installation took 40.240 seconds. The first build exposed and
led to fixes for MapLibre mouse-leave handling, optional schema fields and
null-geometry renderer typing. Geometry-less records remain in counts/tables.

The live original identifies the 1,963-record day as **January 11, 2026**;
source capture and fresh target readback agree. The prior checkpoint's 2025 year
was a reporting error. Source inspection also corrected the earlier spatial
intersection approach: flight selection changes the map complaint highlight,
while the complaint table retains independent day/viewport filtering. The
conversion now follows this separation and implements pointer-following lens
queries with cancellation and debouncing. Those interactions still need target
browser qualification.

Flight item `1b496d1b79fd4344ab548a1f2399c5fa`, chart `1771976572578`, specifies
six-minute intervals aligned to the first timestamp with incomplete intervals
trimmed. Capture SHA-256 is
`c9baf813cdd54a9aa5c3b4f887c120806cfacbb1f7e4bde681eb265ee3331ec3`.
The original N945RF chart visibly plots `177,160,13,0,0,0,125,211`; local binning
matches and accounts for all 734 records, with 48 in the trimmed tail. Empty
COUNT intervals display zero despite the saved null policy; the conversion was
corrected from null gaps to this observed behavior. This is source-chart evidence,
not a claim that target chart interactions have passed.

The dev app at loopback port 18626 reaches the free basemap with attribution and
local service discovery, then rejects the calendar response. Direct server and
installed SDK probes both return `dateofflight: "1/11/2026"` with count 1,963;
ordinary feature queries return the proper ISO date. Both Postgres statistics
readers fall through to culture-dependent DateOnly.ToString(). A server fix and
six culture regressions plus an imported grouped-date query test are written;
the targeted build/tests are running. No locale-guessing workaround was added
to the app. Server PR #4839 remains open; SDK date-only PR #1720 has merged.

The source-aware count changes separately passed 33 Core tests (one existing
skip, zero failures), including selected-source count and exact dedicated-target
count cases. The fresh transitive build/test took 1,777.204 seconds. Its runner
then reached the one-hour wait limit before the Postgres step could begin;
Postgres reconciliation tests, geometry/extent baselines and fresh import
qualification remain pending. Timings exclude queue waits unless stated.

### Candidate calendar, cartography and corridor transport replay

SDK cohort commit `06b8e748a` reproduces the source Speed color variable with
SDK expressions and schema-resolved field names. The five-stop legend, colored
tracks, red complaint symbols and OpenFreeMap attribution render in the browser.
Biome checks 11 files in 1.653 seconds; TypeScript/Vite build passes in 11.288
seconds (633.06 kB gzip main chunk). Automatic WebMap visual-variable conversion
still silently ignores this metadata; Specifica SDK #1723 tracks that product gap.

A separate backend at loopback 18618 overlays the newly compiled Postgres reader
on the protected runtime. Postgres DLL SHA-256 is
`f9b777d83a6fc4c158ac5f582e575692115acbc888b6d95df26e1670f5d3d4b7`.
It returns `2026-01-11` with count 1,963. The app on 18626 now loads all five
aircraft (734, 481, 390, 243 and 115 records), and its N945RF chart visibly matches
the eight original bins. The day table shows 13 complaints in the current view.
This is limited browser evidence, not full interaction qualification.

The first calendar regression build failed after 1,500.181 seconds before tests
ran: preserved original server PID 7524 locks its build-output dependencies.
The retry uses a separate artifacts directory and leaves existing demos running.
Server PR #4839 is draft until the statistics follow-up is qualified. Its earlier
TopFeatures temporal regressions passed in CI for all three JSON paths; the
corresponding review finding was resolved with those TRX results.

Whole-aircraft selection exposed two additional blockers. The installed SDK
builds a 40,245-byte GET request target, rejected by Vite with HTTP 431. Explicit
SDK POST sends the complete 40,177-byte form body and reaches Honua, whose generic
8,192-character input limit rejects geometry with error code 400. Source data and
query geometry were not simplified. SDK #1724 now has a shared transport fix
(`f5ab5c468`); its later CI verification is recorded below. Server #4850 owns
bounded spatial input admission. Both are needed before corridor highlighting
can pass. At that checkpoint the full flight plan was prepared; the subsequent apply and progress are recorded below.

### Bounded geometry implementation and test asset replay

Server commit `3ab624621` declares geometry input metadata on eight layer/service
GET/POST query routes. Shared middleware applies existing configured geometry
byte and vertex limits before query execution. Protocol parsing stays in the
GeoServices module; ordinary text/header limits and injection checks remain in
force. Boundary tests and a routed test with one point inside a detailed corridor
and one outside are written for FeatureServer/MapServer identity/count parity.
Execution, formatting, PR admission and live deployment remain pending.

The isolated calendar build completed in 1,550.672 seconds. All six culture tests
passed; seven import tests failed because RepositoryPaths could not find seed
files above the external artifact directory. A private, hash-verified pack of
154 repository assets (954,320 bytes, including the solution marker, seed SQL and
migrations) now supplies the expected paths. The same compiled tests are queued
for replay with `--no-build --no-restore`; no import-test pass is claimed yet.
The SDK transport lane installed 529 packages in 38.753 seconds and is waiting
for another agent's active build before continuing its checks. Queue waits are
not controlled onboarding performance measurements.

### Full flight import and exact-head SDK transport evidence

The product CLI applied the full flight plan to backend 18617 as job
`f623fecfc1ff`, service `onboarding-heli-flights-full-004`. No date filter or
source simplification was applied; batches contain 1,000 features. The first
600-second resume returned nonterminal with 126,000 processed rows. Later product
status calls observed 143,000 and then 189,000 of 698,843 estimated features,
zero failed rows, at server duration 14 minutes 30.671 seconds. Keep polling this
same job; a polling deadline does not authorize another import. Terminal
publication, count/geometry parity and the app's full date range remain unproven.

SDK CI run `34830168287` at exact commit `f5ab5c4684df55eaa6f736ebf5a75bc41238551f`
passed the build, types/style, four unit coverage shards, merged coverage,
packages/bundles, examples/scaffolds, MCP and four browser groups. All eight new
transport cases were checked individually in shard 2's Vitest artifact. The
separate legacy JS SDK job is still running. The waiting local SDK runner was
stopped after this evidence made its remaining checks redundant; no local test
pass was fabricated. SDK PR #1725 is now ready for review.

The downloaded CI dist digest was recomputed and matches
`a2747df0e5d8d4139cfdc19b242756d47b866ca53fe53c73f1fa32653d11b550`.
Using that compiled client, a synthetic 1,401-position polygon automatically
travels through Vite as a 67,608-byte POST body with a 67-byte request target.
The backend returns HTTP 200 containing GeoServices error 400: geometry exceeds
the generic 8,192-character limit. This isolates remaining server admission work;
it is not installed-package, aircraft-corridor or local-build reuse qualification.

The helicopter worktree's installed dependencies were found missing while Vite
remained alive. The cause has not been independently established. Tracked app
source at `06b8e748a` and its private environment have been copied to the local
onboarding cache outside source control. Its install/check/build runner waits
for the existing calendar and geometry runners and other heavy processes.
The existing runtime has not been replaced, and further browser checks require
the restored runtime. Calendar and geometry checks remain queued behind a
confirmed active architecture-test process owned by another agent.

### Calendar replay result

The corrected private fixture layout now passes all 13 already compiled
calendar/import tests: zero failures and zero skips, 16.677 seconds including
runner overhead. The inspected TRX contains six culture cases across both
statistics readers, the DateOnly import with grouped DATE/count assertions, two
subtype cases and four geometry-dimension cases. Scoped dotnet format is running;
the calendar follow-up remains uncommitted and PR #4839 remains draft until
formatting finishes. The geometry and private-runtime runners are waiting for it.

### Calendar delivery and nonspatial table implementation

Calendar formatting passed in 202.367 seconds. Commit `ef7b55bd3` contains the
qualified four-file grouped-statistics follow-up and is pushed; server PR #4839
is ready for review with its description reconciled to the current change.

A fresh server worktree at trunk `b52bd1dd8` implements nonspatial import and
publication in commit `104055e1e` (pushed). It skips geometry indexes for table
sources, permits validated attribute-only publication, uses canonical Table
resources without spatial fields/CRS, and retains row-count admission. The new
PostGIS regression checks published metadata and source-backed rows, Join_ID,
nulls, paging, and absence of geometry columns or GiST indexes. It also checks
that a table does not acquire an implicit STAC collection. Tests and scoped
formatting are queued; no PR or qualified runtime exists yet. Relationship
mapping, missing-dependency fidelity, rollback coverage and the fresh summary
retry remain required work under server #4834.

The latest full flight job observation is 482,000 of 698,843 features at server
duration 37 minutes 38.758 seconds, zero failed rows. The original job continues;
no replacement import has been queued.

Review of SDK PR #1725 found that automatic POST loses GET retry/auth-refresh
behavior, omits a deployment prefix from its URL budget, and can lose constraints
when redirects downgrade the form POST to GET. The PR has returned to draft.
Local commit `b40c1b58f` now changes the prefix calculation; it was discovered
already committed in the working tree, is not pushed, and has no newly inspected
regression evidence. Preserve it while fixing the remaining replay/redirect
issues. The earlier eight-test CI proof remains tied to `f5ab5c468`; it does not
qualify this later change or the newly discovered cases.

### Full flight transfer completed; publication failed

Job `f623fecfc1ff` is now terminal: 698,843 processed, zero failed rows, elapsed
52m49.366s. Automatic publication failed with an Npgsql read timeout in
`MaterializeLayerFeaturesAsync`. The product nevertheless reports status 8 and
`full-fidelity`, with a warning that publication did not complete. This is not a
completed service migration. Server issue #4854 now requires recovery from the
retained table, accurate outcomes, rollback and idempotence, and public app checks;
#4826 also explicitly rejects an unqualified success when requested publication
is absent. Both are canonical Specifica projections.

Independent SQL confirms 698,843 retained rows, dates 2026-01-11 through
2026-02-16, zero null geometries and 1,963 rows for 2026-01-11. One inspected
geometry is a three-dimensional MultiLineString in SRID 4326. No service row
exists for `onboarding-heli-flights-full-004`. This confirms retained data and the
publication gap; it does not prove all-row attribute/geometry fidelity. Do not
repeat the source import to recover publication.

SDK commit `39ab91b8e` preserves the earlier prefix change and adds narrowly
scoped automatic-query POST retries/auth refresh, cancellation handling and
redirect constraint preservation. It includes 34 new cases covering feature and
map queries, mutation non-replay and encoded prefix boundaries. Scoped Biome and
whitespace checks passed; exact-head tests remain pending. Prior eight-test CI
evidence belongs to `f5ab5c468`, not the new head. SDK PR #1725 remains draft.

Server commit `85b7e9e2b` addresses additional calendar review findings: DateOnly
catalog comparison, normalized field defaults across FeatureServer and MapServer,
and qualified temporal attributes from the right side of dynamic identify joins.
Regression changes exercise mismatch detection, default metadata in three query
paths, and joined identify/dynamic-layer output. Tests and scoped formatting are
queued, and PR #4839 is draft. Earlier `ef7b55bd3` passed PR Gate and all affected
shards; its generated-documents CI gate failed, while local generation/check
produces no diff. The new follow-up is not yet qualified or deployed.

Geometry verification has started in the existing runner. Private app dependency
restore and nonspatial publication tests remain queued behind it. The four-app
goal, full flight publication, editing qualification and repeatable cached/fresh
runs remain incomplete.

### Query verification and publication recovery implementation

The following checkpoint is superseded where noted by the live corridor section
below; earlier execution records are retained as history.

SDK head `39ab91b8e` passed the full SDK Verification graph, run 34836607450.
The individually inspected Vitest blobs record 34 passing replay/auth/redirect/
prefix cases and eight passing original transport cases. Corresponding review
threads are resolved with that evidence; PR #1725 is ready. The separate legacy
JS SDK CI job was still running at the last observation. The downloaded build's
4,314 dist files match producer SHA-256
`d5b31651c72687074f25fcd04e52958f1b01190ffa624e067d943668246f9470`.
The private artifact is staged for later app checks; it is not yet an installed
candidate or full aircraft-corridor qualification.

Server commit `ddc658771`, based on fresh trunk `05041f193`, adds a bounded
canonical snapshot-copy workload timeout (300 seconds by default, configurable
1–3600) and reports requested-but-missing publication as incomplete/NeedsReview.
The canonical snapshot remains because tile paths consume it. Tests cover real
PostgreSQL workload timeout behavior, expiry/rollback, caller cancellation,
invalid budgets, and an actual import whose publisher fails while its row is
retained. Source is pushed; tests and scoped formatting are queued, no PR or
qualified deployment exists yet.

Recovery inspection also found that the public admin PublishLayerRequest omitted
Z/M flags, domains, subtypes and attribute rules accepted by the importer's
internal LayerPublishRequest. Server #4854 explicitly requires retaining these
when present, along with relationships and styling. Full flight publication has
not been retried using a weaker request. The retained 698,843-row table remains
the recovery input.

Calendar PR #4839 subsequently merged current trunk 05041f193 in commit
94ab0094f. Trunk contained updated capability matrices and their generated
concept pages; the branch now passes the 117-page generation check locally.
New-head CI and the already queued calendar review tests remain pending.

### Full corridor query parity through candidate SDK and server

Geometry validation completed: 30 tests passed, zero failed/skipped, and scoped
formatting passed. Server PR [#4856](https://github.com/honua-io/honua-server/pull/4856)
contains the fix and its documented protocol contract at `737a0007e`.
An isolated full managed build is healthy on local port 18629. Existing app and
backend processes were preserved. Both SDK Verification and legacy SDK CI pass
at `39ab91b8e`; the compiled CI artifact was copied beside the registry package
in the private app for this diagnostic, not represented as a registry release.

The complete 734-track half-mile corridor contains 33,113 decoded geometry bytes.
All four ordinary SDK query forms automatically used POST (about 40.2 kB form
bodies) and returned HTTP 200. Source Esri and imported Honua queries return nine
complaints with matching unique complaint keys, all non-object-ID attributes and
EPSG:4326 extents. Each service's ID query independently equals the IDs of its
complete feature rows. No coordinates were simplified or dropped.

The first harness assertion failed because it expected source and target object
IDs to be identical. Imports assign new IDs. The failed receipt remains intact;
the same captured responses were then reconciled using the unique complaint
business key, producing an explicit nine-row source-to-target ID mapping.
This is a harness correction supported by full attribute agreement, not a waived
data difference. Private receipts include `heli-corridor-candidate-parity.json`
and `heli-corridor-candidate-parity-reconciled.json`. Browser selection behavior,
fresh conversion replay and the full flight population remain unqualified.

The pinned private helicopter app also completed npm ci, formatting checks and
its production build. The old Vite process was not replaced. The nonspatial
publication regression runner is executing; calendar, large-publication and
public-API metadata regressions remain queued behind it.

Publication follow-up `c34640847` exposes and forwards dimensions, domains,
subtypes and rules through the admin publication request. Four real API tests
cover 2D/Z/M/ZM retained tables, catalog/public metadata and duplicate rejection;
execution is pending. The disposable recovery connection was registered and its
product health check passed. A private full-flight recovery request preserves Z
metadata; original drawingInfo is retained for style recovery. No publication
retry or source reimport occurred. Full-flight recovery, related-table binding,
Editor CRUD/attachments and repeated fresh/cached runs remain required.

### Nonspatial regression proof and imported editing metadata

The nonspatial publication suite passed 68 cases, zero failed/skipped, at
`104055e1e`. The inspected table case imports into real PostGIS, reads the
activated Table resource and storage-mapped feature reader, retains Join_ID
values and nulls, verifies paging, and confirms no geometry column/index or STAC
publication is fabricated. Scoped formatting remains queued behind another
active build. Public table endpoints, the 877-row retry and relationship fidelity
are still unqualified; no PR is open yet.

Editor follow-up `a21a290da`, based on current trunk `013ffa0d1`, preserves declared
or typed GlobalID bindings through normalized UUID columns and projects attachment
support when copying is requested and a store is configured. Inspection found the
public metadata reader ignored the canonical attachment flag; it now reads that
flag before legacy annotations. FeatureServer exposes visible GlobalID bindings
and their non-editable field type. Public retained-table publication accepts the
same bindings. Source edit permissions are not copied, and the target remains
read-only pending an explicit target edit-enablement workflow.

Four import/persistence cases, seven protocol cases and two public API cases are
written; existing attachment-copy regressions are also queued. No test pass,
deployment or Editor completion is claimed for this new patch. Target CRUD,
attachment inventories/content mapping and browser persistence remain required.

Review of geometry PR #4856 found two additional input gaps: supported JSON and
multipart POST bodies bypass the new geometry budget, and true-curve filters
are rejected by the vertex counter. The PR is back in draft while these are
addressed. The observed form-POST corridor parity remains valid and is explicitly
limited to that path. Neither review finding was resolved without a fix or proof.

### Fresh related-table import and public readback

Scoped formatting passed for the original ten-file nonspatial patch; it was
submitted as server PR #4860 at `66a3217a2`. A private full managed runtime used
provider/core assemblies whose hashes matched the 68-test build.

The first retry, job `248297d653a5`, failed before moving rows because an older
server consumed the shared Redis queue. Logs identify its old spatial-index code.
That receipt is preserved as an environment-isolation failure, not a result for
the patched importer. The candidate was restarted with its own empty Redis logical
database 1; existing servers and their queues were preserved.

Isolated job `1b81e37bd4ef` imported 877 rows with zero failed rows and published
layer 11. Public FeatureServer queries paged every source and target row: counts
were both 877, and all non-object-ID attributes matched exactly, including join
keys and dates. Object IDs are deliberately excluded because import remaps them.
Private evidence: `heli-summary-isolated-public-evidence.json`.

The real run exposed further gaps. Reconciliation incorrectly rejects the table
for having no geometries (NeedsReview). Individual metadata reports Feature Layer
with esriGeometryNull; the destination relationship is absent. Follow-up server
commit `4862d500b` makes source-declared nonspatial geometry/extent checks explicitly
not applicable while retaining count/content checks, lists tables separately in
FeatureServer discovery, and omits spatial metadata/capabilities from table
metadata. Four core cases, the existing real mapped-reader case and a public
admin/FeatureServer test cover the changes; execution and formatting are queued.
PR #4860 is draft until those pass. The deployed runtime is still `66a3217a2`.
Relationship binding, related-record queries, rollback, and a fresh terminal-success
retry remain required; row parity alone does not complete migration.

Geometry review fixes are pushed through `8e1ef33b9` in draft PR #4856. They route
JSON, JSON-object geometry and multipart bodies through the accepted parameter
parser and bound true-curve expansion during densification. CI caught two test
fixture cancellation analyzer errors at `95daad37b`; these were corrected without
suppressing the rule. New core/protocol/security tests and scoped formatting are
queued at the corrected head. Earlier 30-case/form-POST evidence applies only to
the earlier build. Calendar, large-publication/API and editing runners remain
live; full-flight publication, Editor writes/attachments and fresh/cached browser
replays are still outstanding.

The calendar review runner subsequently ended with MSB3027/MSB3021: the original
demo process holds its Debug assemblies open on Windows. No new test pass was
produced. A retry at the same `94ab0094f` source uses separate Release output and
fresh receipts, queued behind existing verification work. The demo stays running.


### Relationship and source-discovery checkpoint (2026-09-14)

Independent source/target reconciliation now covers 579 parents and 877 children:
all join keys match, parent keys are unique, and neither side has orphan children.
Six bounded source related-record requests returned 579 groups and 877 rows that
match the independently captured whole table. The current target still returns
`Relationship 0 not found for layer 7`; related-record migration remains incomplete.

Installed candidate SDK `39ab91b8e` streamed all 877 child rows in four pages and
matched all non-object-ID attributes against the source, including a typed Join_ID
filter. The server remains `66a3217a2`, so this does not qualify the metadata fix.
Private receipts: `heli-summary-sdk-evidence.json`,
`heli-summary-relationship-baseline.json`, and
`heli-summary-related-endpoint-baseline.json`.

Relationship-fidelity correction `962787130` is open as draft server PR #4864.
It captures declarations, including incomplete/composite ones, and makes unapplied
relationships explicit blocking fidelity findings. CI formatting passed; dedicated
Core/PostGIS tests and live qualification remain pending. Batch binding and
post-apply reconciliation still need proof before relationship migration is complete.

Source-root GET evidence found the helicopter `groupbySummary` resource only in
Esri's `tables` array. Discovery at fresh trunk `11fb85d98` reads only `layers`.
Fix `0a233ecb0` discovers both collections and deduplicates IDs, with eleven new
mixed-service, table-only, layer-only, duplicate, failure and cancellation cases.
CI run 34852223654 and direct Core/security regressions were queued; the live fixed
discovery path is not yet qualified. This is REQ-007 in canonical Specifica #4834.

Table CI run 34851083658 found four whitespace errors at `4862d500b`. Correction
`45f3e9ee7` is pushed to PR #4860. The owned local verification runner was confirmed
idle and stopped before changing its pinned source; replacement session 96736
targets the corrected commit. The table worktree is detached to preserve its
verified source while another worktree holds the shared branch ref.

Geometry CI run 34846604543 passed at `8e1ef33b9`: 4458 server Tier=Fast tests and
15 explicitly identified input-validation security cases passed. Both review
threads were resolved with evidence. Direct Core/converter regression suites and
browser/fresh-conversion replay remain pending; PR #4856 remains draft.

Full-flight publication, Editor writes/attachments, reviewed relationship binding,
and complete fresh/cached browser replay are still required. None of these source
and readback checks establishes completion of the four-app conversion.
